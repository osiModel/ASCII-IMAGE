#ifndef IDENTICAL_HPP
#define IDENTICAL_HPP

#include <cstdint>
#include <vector>
#include <utility>

namespace{
    using std::pair; 
    using std::vector;
    using Pos = pair<uint16_t,uint16_t>; 
    using Pixel = bool;
    using Map = vector<vector<Pixel>>;
}

#endif // IDENTICAL_HPP
